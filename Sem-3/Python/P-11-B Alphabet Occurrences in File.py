# P-11-B Write a program that reads a text file and counts the occurrences of each alphabet in the file. The program should prompt the user to enter the filename.

f=open("test.txt","r")
print(f.read())
f.close()
